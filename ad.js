let m = '0'; // Assigning a value to variable m

if (m === '0') {
    // This block will execute because m is exactly equal to '0'
    console.log('m is equal to \'0\'');
} else {
    // This block would execute if the condition is false
    console.log('m is not equal to \'0\'');
}
